import os

const 
  pilRootPath* = currentSourcePath.parentDir().parentDir()

const
  PPROJECT_INFO_FILE*    = "pproject.json"
  PPACKAGE_INFO_FILE*    = "ppackage.json"
  PMODULE_INFO_FILE*     = "pmodule.json"
